import Axios from 'axios'

const baseURL = "/baseMongoDb"

class loginService {
 
 getUsers (user) {
    /* try {
        const response = Axios.get(baseURL,user.usuario)
        return response
    } catch (e) {
        console.log(e)
    } */
}

}

export default new loginService 



